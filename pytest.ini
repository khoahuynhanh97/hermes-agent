[tool:pytest]
markers =
    unit: Unit tests for individual components
    integration: Integration tests between components
    contract: Contract tests verifying interfaces and contracts
    migration: Migration tests for data and schema changes
    web: Web interface tests including API and UI

# Use a deterministic temporary directory base for reproducible tests
testpaths = tests
tmpdir_handler = lib